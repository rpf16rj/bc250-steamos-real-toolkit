--- a/drivers/gpu/drm/amd/pm/swsmu/smu11/cyan_skillfish_ppt.c	2026-09-17 22:09:38.246984240 -0300
+++ b/drivers/gpu/drm/amd/pm/swsmu/smu11/cyan_skillfish_ppt.c	2026-09-17 22:09:38.319150363 -0300
@@ -135,6 +135,14 @@
 		 "Force 8-core SMU metrics layout");
 
 /*
+ * Set by drivers/gpu/drm/amd/display/dc/link/protocols/link_hdmi_frl.c while
+ * HDMI FRL link training (boot-time or a later SCDC-triggered retrain) is in
+ * progress. See the comment there for why OD commits need to defer around it.
+ */
+extern atomic_t cs_hdmi_frl_training_active;
+extern bool cs_od_defer_debug;
+
+/*
  * On an 8-core board, decode the metrics table the way the pre-patch SMU
  * firmware laid it out. Only needed on an older BIOS that unlocks the two
  * extra cores without carrying the 8-core metrics firmware patch; with that
